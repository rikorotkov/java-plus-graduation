server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  cloud:
    gateway:
      routes:
        - id: stat-server
          uri: lb://stats-server
          predicates:
            - Path=/**

        - id: main-service
          uri: lb://main-service
          predicates:
            - Path=/**